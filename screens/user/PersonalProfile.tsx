import React from 'react';
import { View } from 'react-native';
import { Text } from 'react-native-elements';

interface Props {};

const PersonalProfile: React.FC<Props> = () => {

  return (
    <View>
      <Text>Personal Profile landing</Text>
    </View>
  )
};

export default PersonalProfile;